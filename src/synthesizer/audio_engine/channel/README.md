## This is the MIDI channel folder.

The code here is responsible for managing a single MIDI channel (part).

- `parameters` - the Global MIDI and System parameters.

For those interested, `render_voice.ts` file contains the actual DSP synthesis code.
