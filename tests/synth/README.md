# This is the synthesizer tests' folder.

For some of these, you need to `npm install` the `speaker` package.
It is not a devDependency because it has a high vulnerability that has been unresolved for a long time.
