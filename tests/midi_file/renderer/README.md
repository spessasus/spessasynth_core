# This is the C++ VST renderer folder.

It contains the code for a C++ renderer which can render
a MIDI file to WAV with a VST plugin using the BASS library.
Run `npm run build:renderer` to build the renderer.

Note that `CMakeLists.txt` should not be used to build the code,
it's just for IDE support.
